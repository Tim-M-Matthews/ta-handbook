---
title: Clinical
order: 60
roles:
  - clinical
---

**Category-level roles:** only users with the `clinical` role (or `admin`) can see *any* page whose frontmatter `category` is `clinical`, regardless of page-level `roles`. Use GitCMS to edit this file and reorder or tighten access for the whole section.
