---
title: General
order: 1000
---

Default bucket for pages whose `category` is missing or does not match a file under `content/categories/`. You can delete this file if you always set a real category id on every page.
